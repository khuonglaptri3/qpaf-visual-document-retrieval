# Experiment configurations

Chưa có cấu hình thí nghiệm được cung cấp hoặc chốt. Khương nhập cấu hình
thật sau khi Phát chốt protocol; mỗi run phải ghi config path/hash và commit.
QARF/QPAF dùng shared-base config, với khác biệt theo quy định của protocol.
README này không phải một experiment config trong M1.4.
